import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//"eslint": "^7.5.0",

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root'),
);
